<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>TinyApp</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  
</head>

<body>
  <!-- Include the header made from _header.ejs -->
  <%- include('partials/_header') %>
  <main style="margin: 1em;">
    <h3>My URLs</h3>
    <table class="table">
      <thead> 
        <tr>
          <th scope="col">Short URL</th>
          <th scope="col">Long URL</th>
          <th scope="col">Edit</th>
          <th scope="col">Delete</th>
        </tr>
      </thead>
      <tbody>
        <!-- for each url in urls access its [url] key and display it -->
        <!-- here the url is from the urlDatabase that is exported from express_server.js -->
        <% for(let url in urls) { %>
          <tr>
            <!-- here the url is the key of the urlDatabase (from templateVar) which represents the shortURL-->
            <td><%= url %></td>
            <!-- here the urls[urls] is the longURL -->
            <td><%= urls[url].longURL %></td>
            <td>
              <!-- when you click on this button read (GET) info for that shorturl (<%= url %>) using the action path 
                   this then renders the urls_show.ejs from the express_server.js [app.get("/urls/:shortURL"]-->
              <form method="GET" action="/urls/<%= url %>">
                <button type="submit">Edit</button>
              </form>
            </td>
            <td>
              <!-- when you click on this button update (POST) info for that shorturl (<%= url %>/delete) using the action path 
                this then redirects to "/urls"  from the express_server.js [app.post("/urls/:shortURL/delete"]-->
              <form method="POST" action="/urls/<%= url %>/delete">
                <button type="submit">Delete</button>
              </form>
            </td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </main>
   <!-- Bootstrap JS -->
   <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
   <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
   <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
</body>
</html>